import { useEffect } from "react";

const Cookies = () => {
  useEffect(() => {
    window.location.replace(
      "https://www.iubenda.com/privacy-policy/46903805/cookie-policy",
    );
  }, []);

  return null;
};

export default Cookies;
