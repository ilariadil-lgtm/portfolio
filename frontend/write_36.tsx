"# Add English Language Support to \"Editorial\" Mode\n\nThe goal is to introduce English language support to the \"Editorial\" pages and shared components, allowing users to toggle between Italian (default) and English. \n\n## Open Questions\n\n> [!IMPORTANT]\n> - Should I use `react-i18next` for translations, storing texts in JSON files? \n> - Given the high number of pages (23 in `editorial/` + shared components), I will focus on migrating and translating the core pages first (like `Index`, `Chisono`, `Progetti`, `Servizi`, `Contatti`) and a few project details to establish the pattern, before tackling the rest. Is that acceptable, or should I do *all* of them in one go?\n> - Where should the language switcher (IT | EN) be placed? My proposal is to put it in the `Navigation` header.\n> - What about data fetched from the API (e.g. `api.getProjects()`)? Should I also implement translations on the Django backend side or focus only on hardcoded frontend text for now?\n\n## Proposed Changes\n\n### Setup i18n infrastructure\n- Add `i18next` and `react-i18next` to `package.json`.\n- Create `src/lib/i18n.ts` to initialize i18next.\n- Create translation files: `src/locales/it.json` and `src/locales/en.json` (or separate them by namespace/page to avoid massive files).\n- Wrap the app with `I18nextProvider` (if necessary) in `App.tsx` or `main.tsx`.\n\n### Update Navigation\n#### [MODIFY] `frontend/src/components/Navigation.tsx`\n- Add a language switcher toggle (IT | EN).\n- Translate navigation items (Home, Chi Sono, Servizi, Progetti, Parliamo).\n\n### Translate Shared Components\n#### [MODIFY] `frontend/src/components/Footer.tsx`\n#### [MODIFY] `frontend/src/components/CreativeHero.tsx`\n#### [MODIFY] `frontend/src/components/BriefingCTA.tsx`\n- Extract text into translation JSONs and replace with `t('key')`.\n\n### Translate Editorial Pages\n#### [MODIFY] `frontend/src/pages/editorial/Index.tsx`\n#### [MODIFY] `frontend/src/pages/editorial/Chisono.tsx`\n#### [MODIFY] `frontend/src/pages/editorial/Progetti.tsx`\n####
<truncated 912 bytes>